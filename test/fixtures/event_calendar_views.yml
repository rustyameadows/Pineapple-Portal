vendor_view:
  event_calendar: run_of_show
  name: Vendor Only
  slug: vendor-only
  description: Timeline for vendor partners.
  tag_filter:
    - <%= ActiveRecord::FixtureSet.identify(:vendor) %>
  hide_locked: true
  client_visible: true
  position: 0

decision_view:
  event_calendar: run_of_show
  name: Decision Calendar
  slug: decision-calendar
  description: Key decisions and approvals.
  hide_locked: false
  client_visible: true
  position: 1
