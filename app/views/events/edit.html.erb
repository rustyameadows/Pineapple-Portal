<% provide :page_shell do %>
  <div class="event-layout event-layout--index">
    <%= render "events/sidebar_index" %>

    <div class="event-layout__main">
      <%= render "shared/flash" %>

      <section class="event-section">
        <header class="event-section__header">
          <div>
            <p class="event-section__eyebrow">Events</p>
            <h1 class="event-section__title">Edit Event</h1>
            <p class="event-section__lead">Update core event details and schedule dates.</p>
          </div>
          <div class="event-section__actions">
            <%= link_to "Back to event", event_path(@event), class: "event-section__link" %>
          </div>
        </header>

        <div class="event-section__body">
          <%= render "form",
                    submit_label: "Save Changes",
                    cancel_path: event_path(@event),
                    cancel_label: "Cancel" %>
        </div>
      </section>
    </div>
  </div>
<% end %>
