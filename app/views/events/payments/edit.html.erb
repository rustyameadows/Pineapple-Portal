<% provide :page_shell do %>
  <div class="event-layout">
    <%= render "events/sidebar_navigation", event: @event %>

    <div class="event-layout__main">
      <%= render "shared/flash" %>

      <section class="event-section event-payments event-payments--edit">
        <header class="event-section__header">
          <div>
            <p class="event-section__eyebrow">Payments</p>
            <h1 class="event-section__title">Edit Payment</h1>
            <p class="event-section__subtitle">Update payment status, due timing, and client-facing details for this payment item.</p>
          </div>
        </header>

        <%= render "form", payment: @payment %>
      </section>
    </div>
  </div>
<% end %>
