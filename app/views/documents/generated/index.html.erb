<% provide :page_shell do %>
  <div class="event-layout">
    <%= render "events/sidebar_navigation", event: @event %>

    <div class="event-layout__main">
      <%= render "shared/flash" %>

      <section class="event-section">
        <header class="event-section__header">
          <div>
            <p class="event-section__eyebrow">Generated documents</p>
            <h1 class="event-section__title">Assemble branded packets for <%= @event.name %></h1>
            <p class="event-section__lead">Reuse curated sections and uploaded PDFs to publish polished packets for clients and partners.</p>
          </div>
          <%= link_to "New generated document", new_event_documents_generated_path(@event), class: "event-section__cta" %>
        </header>

        <div class="event-section__body">
          <%= render "documents/browser",
                    items: @manifest_entries,
                    title_for: ->(entry) { entry[:definition].title },
                    href_for: ->(entry) { event_documents_generated_path(@event, entry[:logical_id]) },
                    table_columns: [
                      {
                        label: "Last compiled",
                        value: ->(entry) do
                          latest = entry[:latest]
                          latest.present? && latest.storage_uri.present? ? latest.updated_at.to_fs(:short) : "Not yet compiled"
                        end
                      },
                      {
                        label: "Compiled versions",
                        value: ->(entry) { entry[:versions].count(&:storage_uri) }
                      },
                      {
                        label: "Visibility",
                        value: ->(entry) do
                          definition = entry[:definition]
                          content_tag(
                            :span,
                            definition.client_visible? ? "Client visible" : "Internal only",
                            class: "documents-browser__pill #{definition.client_visible? ? "documents-browser__pill--visible" : "documents-browser__pill--muted"}"
                          )
                        end
                      }
                    ],
                    card_fields: [
                      {
                        label: "Last compiled",
                        value: ->(entry) do
                          latest = entry[:latest]
                          latest.present? && latest.storage_uri.present? ? latest.updated_at.to_fs(:short) : "Not yet compiled"
                        end
                      },
                      {
                        label: "Compiled versions",
                        value: ->(entry) { entry[:versions].count(&:storage_uri) }
                      },
                      {
                        label: "Visibility",
                        value: ->(entry) { entry[:definition].client_visible? ? "Client visible" : "Internal only" }
                      }
                    ],
                    empty_message: "No generated documents yet.",
                    search_placeholder: "Search by file name",
                    title_heading: "Title" %>
        </div>
      </section>
    </div>
  </div>
<% end %>
