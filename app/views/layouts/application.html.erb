<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pineapple Portal" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <% planner_stylesheets = %w[
      base/tokens
      base/reset
      base/elements
      utilities/helpers
      utilities/responsive
      layout/app-shell
      layout/planner-shell
      layout/sidebar
      components/flash
      components/cards
      components/buttons
      components/forms
      components/tables
      components/pills
      components/tags
      components/modal
      components/event-links
      components/timeline
      components/calendar-grid
      components/documents-table
      components/payments-list
      components/approvals-list
      pages/events-index
      pages/events-show
      pages/events-calendar
      pages/events-payments
      pages/events-approvals
      pages/events-settings
      pages/questionnaires
      pages/documents
      pages/payments
      pages/approvals
    ] %>
    <% planner_stylesheets.each do |logical_path| %>
      <%= stylesheet_link_tag logical_path, "data-turbo-track": "reload" %>
    <% end %>
  </head>

  <body>
    <% if content_for?(:page_shell) %>
      <%= yield :page_shell %>
    <% else %>
      <nav>
        <%= link_to "Pineapple Portal", root_path %> |
        <%= link_to "Events", events_path %> |
        <%= link_to "Templates", questionnaire_templates_path %> |
        <%= link_to "Team", users_path %>
        <% if logged_in? %>
          | Signed in as <%= current_user.name %>
          | <%= button_to "Log out", logout_path, method: :delete, form: { style: "display:inline" } %>
        <% else %>
          | <%= link_to "Log in", login_path %>
        <% end %>
      </nav>

      <%= render "shared/flash" %>

      <%= yield %>
    <% end %>
    <%= yield :scripts %>
  </body>
</html>
